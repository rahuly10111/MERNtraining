import registrationRepositary from './registrationRepository/registrationRepository';
import loginRepositary from './loginRepository/loginRepository';
import tokenRepository from './tokenUserDetailRepository/tokenUserDetailRepository'
import userProfilerepository from './userProfilerepository/userProfilerepository'
import postrepository from './postrepository/postrepository'
import commentsrepository from './commentsrepository/commentsrepository'
import categoryrepository from './categoryrepository/categoryrepository'

export default {
    registrationRepositary,
    loginRepositary,
    tokenRepository,
    userProfilerepository,
    postrepository,
    commentsrepository,
    categoryrepository
}